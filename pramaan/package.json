{
  "name": "et-nucleus",
  "version": "1.0.0",
  "private": true,
  "description": "ET Nucleus — Intelligence-First, Persona-Aware Business News. Single-server (Express + built React) deploy.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "./start.sh"
  }
}
